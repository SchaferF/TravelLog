import { SearchPlaceResponse } from './search-place-response';

describe('SearchPlaceResponse', () => {
  it('should create an instance', () => {
    expect(new SearchPlaceResponse()).toBeTruthy();
  });
});
