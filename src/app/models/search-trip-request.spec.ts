import { SearchTripRequest } from './search-trip-request';

describe('SearchTripRequest', () => {
  it('should create an instance', () => {
    expect(new SearchTripRequest()).toBeTruthy();
  });
});
