import { SearchTripResponse } from './search-trip-response';

describe('SearchTripResponse', () => {
  it('should create an instance', () => {
    expect(new SearchTripResponse()).toBeTruthy();
  });
});
