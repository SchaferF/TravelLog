import { SearchPlaceRequest } from './search-place-request';

describe('SearchPlaceRequest', () => {
  it('should create an instance', () => {
    expect(new SearchPlaceRequest()).toBeTruthy();
  });
});
