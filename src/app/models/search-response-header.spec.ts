import { SearchResponseHeader } from './search-response-header';

describe('SearchResponseHeader', () => {
  it('should create an instance', () => {
    expect(new SearchResponseHeader()).toBeTruthy();
  });
});
