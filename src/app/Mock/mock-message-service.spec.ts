import { MockMessageService } from './mock-message-service';

describe('MockMessageService', () => {
  it('should create an instance', () => {
    expect(new MockMessageService()).toBeTruthy();
  });
});
